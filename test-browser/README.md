# ブラウザ向けのテスト

このディレクトリには、ブラウザ向けのテストコードが含まれています。これらのテストは、ブラウザ環境での動作を確認するために使用されます。

このフォルダ以下のブラウザテストは、Playwright + ヘッドレス Chromium で実行します。

テストを準備するために、以下のコマンドを実行します。

```sh
cd test-browser
npm install
```

テストを実行するには、以下のコマンドを使用してください。

```sh
npm run test
```
