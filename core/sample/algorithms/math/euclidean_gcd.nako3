# ユークリッドの互除法
# 入力: 2つの整数
# 出力: 2数の最大公約数（常に0以上）
# 計算量: O(log(min(A, B)))

●(AとBの)最大公約数計算処理とは:
    もし、A<0ならば:
        Aは0-A
    もし、B<0ならば:
        Bは0-B
    (B!=0)の間:
        変数のF余りはA%B
        AはB
        BはF余り
    Aを戻す
